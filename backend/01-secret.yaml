# Template only — do not fill in real values and commit this file.
# Create the real secret directly instead, e.g.:
#   kubectl create secret generic thanes-lims-secrets -n thanes-lims \
#     --from-literal=DATABASE_URL='postgres://user:pass@host:5432/thanes_lims?sslmode=require' \
#     --from-literal=JWT_ACCESS_SECRET='...' \
#     --from-literal=JWT_REFRESH_SECRET='...' \
#     --from-literal=MINIO_ACCESS_KEY='...' \
#     --from-literal=MINIO_SECRET_KEY='...'
# or manage it with your secrets tooling (sealed-secrets, external-secrets, etc).
apiVersion: v1
kind: Secret
metadata:
  name: thanes-lims-secrets
  namespace: thanes-lims
type: Opaque
stringData:
  DATABASE_URL: "postgres://CHANGEME:CHANGEME@CHANGEME:5432/thanes_lims?sslmode=require"
  JWT_ACCESS_SECRET: "CHANGEME"
  JWT_REFRESH_SECRET: "CHANGEME"
  MINIO_ACCESS_KEY: "CHANGEME"
  MINIO_SECRET_KEY: "CHANGEME"
